# Application Configuration
NODE_ENV=development
PORT=3000

# CORS Configuration
# Comma-separated list of allowed origins
# Production: Set to your frontend domain(s)
# Development: http://localhost:3000,http://localhost:3001
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Secrets Management Configuration
# Options: env, aws, azure, vault
# env: Load from environment variables (development only)
# aws: Load from AWS Secrets Manager
# azure: Load from Azure Key Vault
# vault: Load from HashiCorp Vault
SECRETS_PROVIDER=env

# AWS Secrets Manager (when SECRETS_PROVIDER=aws)
# AWS_REGION=us-east-1
# AWS_SECRET_NAME=purpose-agnostic-agent/prod

# Azure Key Vault (when SECRETS_PROVIDER=azure)
# AZURE_KEY_VAULT_URL=https://your-vault.vault.azure.net

# HashiCorp Vault (when SECRETS_PROVIDER=vault)
# VAULT_ADDR=http://localhost:8200
# VAULT_TOKEN=your-vault-token
# VAULT_SECRET_PATH=secret/data/purpose-agnostic-agent

# Authentication Configuration
# JWT secret key - CHANGE THIS IN PRODUCTION!
# Generate with: openssl rand -base64 32
JWT_SECRET=change-me-in-production-use-strong-secret
JWT_EXPIRES_IN=24h

# API Keys for MCP and service-to-service auth
# Comma-separated list of valid API keys
# Generate with: openssl rand -hex 32
API_KEYS=pak_your_api_key_here

# Admin Panel Configuration
# Admin JWT secret - CHANGE THIS IN PRODUCTION!
# Should be different from JWT_SECRET for security
# Generate with: openssl rand -base64 32
ADMIN_JWT_SECRET=change-me-in-production-use-different-secret
ADMIN_JWT_EXPIRATION=8h

# Admin rate limiting for login attempts (prevents brute force)
# Maximum login attempts within the time window
ADMIN_RATE_LIMIT_MAX=5
# Time window in milliseconds (900000ms = 15 minutes)
ADMIN_RATE_LIMIT_WINDOW=900000

# Database Configuration
# PostgreSQL connection string with pgvector support
DATABASE_URL=postgresql://postgres:password@localhost:5432/purpose_agnostic_agent

# Redis Configuration
# Redis connection string for BullMQ job queue
REDIS_URL=redis://localhost:6379

# LLM Provider Configuration
# Google AI Studio API key (Primary - Free tier available)
# Get your key from: https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# OpenRouter API key for GPT-4o and Claude-3.5 access (Fallback)
# Get your key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# OpenAI API key for embeddings (text-embedding-3-small)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Ollama Configuration
# Local Ollama instance URL for fallback LLM
OLLAMA_URL=http://localhost:11434

# Usage Tracking Configuration (Optional - for free tier limits)
# Enable usage tracking to stay within API limits
# Set to 'false' to disable tracking (for paid tiers)
USAGE_TRACKING_ENABLED=true

# Gemini Free Tier Limits (adjust based on your plan)
# Free tier: 15 RPM, 1500 requests/day, 1M tokens/day
DAILY_REQUEST_LIMIT=1500
DAILY_TOKEN_LIMIT=1000000
RPM_LIMIT=15

# Logging Configuration
# Log level: error, warn, info, debug
LOG_LEVEL=info

# RAG Configuration
# Enable self-check to verify answers use only context (optional, adds latency)
# Set to 'true' to enable consistency validation
RAG_SELF_CHECK_ENABLED=false

# Seq Configuration (Optional - can be replaced with Datadog, New Relic, etc.)
# Seq server URL for centralized log aggregation
# Access Seq UI at http://localhost:5341
# To disable: leave SEQ_URL empty
# Alternatives: DATADOG_API_KEY, NEW_RELIC_LICENSE_KEY, ELASTIC_URL
SEQ_URL=http://localhost:5341
SEQ_API_KEY=

# Prometheus Configuration (Optional - can be replaced with Datadog, New Relic, etc.)
# Enable Prometheus metrics collection
# Access Prometheus at http://localhost:9090
# To disable: set to false
PROMETHEUS_ENABLED=true

# Storage Configuration
# Storage type for personas: database or file
STORAGE_TYPE=database

# Persona Configuration
# Path to personas JSON configuration file (when using file storage)
PERSONA_CONFIG_PATH=./config/personas.json
